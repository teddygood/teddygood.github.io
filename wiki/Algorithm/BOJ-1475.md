---
title: "BOJ 1475"
date: '2025-11-12 14:12'
---
## [1475 방 번호](https://www.acmicpc.net/problem/1475)

:::tip
Clicking the heading will take you to the BOJ problem.
:::

## Solution

6과 9를 어떻게 동일하게 처리할 것인지가 포인트. 단순 수학이지만 바로 생각이 나지 않았다.

```python
num = input()
count = [0] * 10

for c in num:
    count[int(c)] += 1

count[6] = (count[6] + count[9] + 1) // 2
count[9] = 0  

print(max(count))
```

```cpp
#include <iostream>

using namespace std;

int main() {
    // 문자열로 입력 받는 방법
    // string s;
    // cin >> s;

    // int cnt[10] = {0};

    // // 숫자 개수 세기
    // for (char c : s) {
    //     cnt[c - '0']++;
    // }

    // // 6과 9는 함께 사용 가능
    // int sixNine = cnt[6] + cnt[9];
    // cnt[6] = cnt[9] = (sixNine + 1) / 2;  // 올림 처리

    // // 가장 많이 필요한 숫자가 필요한 세트 수
    // int answer = 0;
    // for (int i = 0; i < 10; i++) {
    //     if (cnt[i] > answer) answer = cnt[i];
    // }

    // cout << answer;
    // return 0;

    int n;
    int a[10] = {};
    int ans = 0;
    cin >> n;

    while (n) {
        a[n % 10]++;
        n /= 10;
    }

    for (int i = 0; i < 10; i++) {
        if (i == 6 || i == 9)
            continue;
        ans = max(ans, a[i]);
    }
    ans = max(ans, (a[6] + a[9] + 1) / 2);
    cout << ans;
}
```