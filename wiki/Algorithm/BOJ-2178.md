---
title: "BOJ 2178"
date: '2025-12-07 22:51'
---
## [2178 미로 탐색](https://www.acmicpc.net/problem/2178)

:::tip
Clicking the heading will take you to the BOJ problem.
:::

## Solution
바킹독님의 강의 듣다가 풀게 된 문제. 띄어쓰기가 없어서 `string`으로 받아야 하는 게 신기했다.

```cpp
#include <iostream>
#include <queue>

#define X first
#define Y second

using namespace std;

string board[101];
int dist[101][101];

int n, m;

int dx[4] = {1, 0, -1, 0};
int dy[4] = {0, 1, 0, -1};

int main() {
    cin >> n >> m;
    for (int i = 0; i < n; i++) {
        cin >> board[i];
    }
    for (int i = 0; i < n; i++) {
        fill(dist[i], dist[i] + m, -1);
    }
    queue<pair<int, int>> Q;
    Q.push({0, 0});
    dist[0][0] = 0;
    while (!Q.empty()) {
        auto cur = Q.front();
        Q.pop();
        for (int dir = 0; dir < 4; dir++) {
            int nx = cur.X + dx[dir];
            int ny = cur.Y + dy[dir];
            if (nx < 0 || nx >= n || ny < 0 || ny >= m)
                continue;
            if (dist[nx][ny] >= 0 || board[nx][ny] != '1')
                continue;
            dist[nx][ny] = dist[cur.X][cur.Y] + 1;
            Q.push({nx, ny});
        }
    }
    cout << dist[n - 1][m - 1] + 1; 
}
```